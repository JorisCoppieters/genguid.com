// ******************************
// Declarations:
// ******************************

export function isPositiveInteger(in_value: number) {
    return Number.isInteger(in_value) && in_value >= 0;
}

// ******************************
